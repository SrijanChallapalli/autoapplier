# Optional: enables LLM-assisted job parsing and match reasoning.
# Without this, AutoApplier still works using a deterministic scoring engine.
# Get a key from the Vercel AI Gateway (https://vercel.com/docs/ai-gateway).
AI_GATEWAY_API_KEY=

# Model used for parsing/matching when a key is present.
AI_MODEL=anthropic/claude-sonnet-5
